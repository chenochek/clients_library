const Urls = () => {
   // const domain = `http://localhost:3000`;
    const domain = `http://93.125.114.118:3000/`
    return {
        'clients': `${domain}/clients`,
        'visits': `${domain}/visits`,
        'visit': `${domain}/visit`,
        'convert': `${domain}/images/convert/webp`,
        'download': `${domain}/images/download/jpeg`,
        'image': `${domain}/images`,
        'birthday': `${domain}/birthday`

    }

};


export default Urls